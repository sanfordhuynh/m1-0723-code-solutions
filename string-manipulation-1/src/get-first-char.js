/* exported getFirstChar */

/**
 * Define a function called "getFirstChar" that accepts one argument 'string'
 * Return the charAt() method of the string object with 1 argument that takes the first index of the string
 *
 */
function getFirstChar(string) {
  return string.charAt(0);
}
